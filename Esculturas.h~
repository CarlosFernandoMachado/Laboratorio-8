#pragma once
#include "Obras.h"
#include <string>

using std::string;

class Esculturas : public Obras{
	double peso;
	string material;
public:
	Esculturas(double,string);
	~Esculturas();
	string toString();
};
