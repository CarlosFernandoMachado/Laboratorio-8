#pragma once
#include "Obras.h"
#include <string>

using std::string;

class Literatura : public Obras{
	string genero;
	string epoca;
public:
	Literatura(string,string);
	~Literatura();
	string toString();
};
